import React from "react";
import ReactDOM from "react-dom/client";
import DocuTrackApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DocuTrackApp />
  </React.StrictMode>
);
